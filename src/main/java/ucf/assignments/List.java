package ucf.assignments;

public class List
{
    List(String title)
    {
        //creates a new todo list with the given title
        //instantiate an empty array list of list items
    }
    public void addItem(ListItem item)
    {
        //adds a new ListItem object to the classwide array list of list items
    }
    public void removeItem(int index)
    {
        //removes a new ListItem object from the classwide array list of list items

    }
    public String save()
    {
        //returns a string representation of the list object as well as the listItem items within the array list
        return "";
    }
}
